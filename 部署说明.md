# 云匠引擎 - 前后端对接部署说明

## 改动概要

### 后端 server.py（新版）
- **新增核心接口** `POST /api/v1/stream`：流式LLM代理，前端每一步调用此接口
- **新增专家接口** `POST /api/v1/expert/run`：专家感知流式（自动加载知识库+对话历史）
- **保留兼容接口**：`/api/v1/create`（异步）、`/api/v1/step/{expert}`（同步）、进度查询、专家列表
- SSE事件格式：`chunk`（文本块）、`done`（完成）、`error`（错误）
- 支持通过环境变量配置LLM

### 前端 demo-v6-final.html
- `callLLM()` 后端模式改为调用 `/api/v1/stream`
- 请求体改为OpenAI标准messages格式
- 不再依赖stepNum判断是否走后端模式

## 快速启动

### 1. 安装依赖

```bash
pip install fastapi uvicorn httpx pydantic
```

### 2. 配置环境变量

```bash
# 必填：LLM API Key
export OPENAI_API_KEY="sk-your-key-here"

# 可选：自定义API地址（默认OpenAI）
export OPENAI_BASE_URL="https://api.openai.com/v1"
# 如果用其他兼容OpenAI的API（如硅基、DeepSeek等）：
# export OPENAI_BASE_URL="https://api.siliconflow.cn/v1"

# 可选：模型名称
export LLM_MODEL="gpt-4o-mini"
```

### 3. 启动后端

```bash
cd drama-engine/src/api/
python server.py
```

默认监听 `http://0.0.0.0:8000`

### 4. 配置前端

在云匠引擎Demo右上角 ⚙️ 设置中：
- **后端API地址** 填入：`http://localhost:8000`
- **API Key** 可以留空（后端已配置）

### 5. 验证

```bash
# 健康检查
curl http://localhost:8000/health

# 流式测试
curl -X POST http://localhost:8000/api/v1/stream \
  -H "Content-Type: application/json" \
  -d '{"messages": [{"role": "user", "content": "你好"}]}'
```

## API接口文档

### POST /api/v1/stream （核心）

流式LLM代理，前端每一步调用此接口。

**请求体：**
```json
{
  "messages": [
    {"role": "system", "content": "你是..."},
    {"role": "user", "content": "请帮我..."}
  ],
  "model": "gpt-4o-mini",    // 可选，不传用环境变量默认
  "temperature": 0.8,         // 可选
  "max_tokens": 4096          // 可选
}
```

**SSE响应：**
```
data: {"type": "chunk", "content": "你"}
data: {"type": "chunk", "content": "好"}
data: {"type": "chunk", "content": "！"}
data: {"type": "done", "total_tokens": -1}
```

### POST /api/v1/expert/run

专家感知流式接口，根据expert_id自动构建prompt。

**请求体：**
```json
{
  "expert_id": "soul_catcher",
  "user_input": "我想做一个关于云南白族的故事",
  "conversation_history": [
    {"role": "assistant", "content": "你好！请告诉我你的创意..."},
    {"role": "user", "content": "我想做白族扎染"}
  ],
  "context": {"story_direction": "..."}
}
```

### GET /health

健康检查，返回LLM配置状态。

### GET /api/v1/experts

列出所有17个专家。

## 四节点交互模式（下一步）

当前对接完成后，下一步改造为4节点交互：
1. 故事大纲（5专家）
2. 人物小传（2专家）
3. 集纲（3专家）
4. 正剧剧本（7专家）

每个节点完成后用户评审/修改/通过，详见《四节点交互改造方案.md》
